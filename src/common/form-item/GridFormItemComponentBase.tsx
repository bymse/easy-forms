import React, {ReactNode} from "react";
import {FormItemType} from "./IFormItem";

type Props = {type: FormItemType, children: ReactNode};

export function GridFormItemComponentBase(props: Props) {
    
}